{
  "name": "vanilla",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -r pictures dist/assets/pictures",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^3.2.5"
  },
  "dependencies": {
    "gh-pages": "^4.0.0",
    "three": "^0.146.0"
  }
}
